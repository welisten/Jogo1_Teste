const GameWidth = 368 
const GameHeight = 1920

const ScreenWidth = window.innerWidth - 20
const ScreenHeight =  window.innerHeight - 20

export {
    GameWidth,
    GameHeight,
    ScreenWidth,
    ScreenHeight
}